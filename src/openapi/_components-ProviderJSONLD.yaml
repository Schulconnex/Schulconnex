title: Provider JSON+LD Schema
type: object
required:
  - identifier
properties:
  "@type":
    type: string
    enum:
      - "Organization"
      - "Person"
    default: Organization
  identifier:
    type: string
    format: uri
    example: https://exampleprovider.tld

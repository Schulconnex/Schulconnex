type: object
required:
  - action
properties:
  assigner:
    type: string
    format: uri
  assignee:
    type: string
    format: uri
  assignee_scope:
    type: string
    format: uri
  target:
    type: string
    format: uri
  output:
    type: string
    format: uri
  action:
    type: string
    format: uri
  constraints:
    type: array
    items:
      $ref: "./components-Constraint.yaml"
